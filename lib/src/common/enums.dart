enum FieldValue {
  email,
  password;
}

enum BottomNavigationOption {
  home,
  category,
  orders,
  account,
}
enum VerificationType{
  register,
  forgot;
}

enum CategoryType {
  category,
  viewAll
}

enum OrderType {
  normalOrder,
  customOrder
}